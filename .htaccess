# .htaccess corrigé pour Render
Options -Indexes
RewriteEngine On

# IMPORTANT : Render gère déjà HTTPS, PAS de redirection HTTP→HTTPS ici !
# Supprime toute ligne avec RewriteCond %{HTTPS} off

# URLs propres (sans redirection HTTPS)
RewriteRule ^product/([0-9]+)/([a-zA-Z0-9-]+)$ product.php?id=$1&slug=$2 [L,QSA]

# Protection des fichiers sensibles
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

<FilesMatch "config\.php">
    Order allow,deny
    Deny from all
</FilesMatch>

# Compression Gzip
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css text/javascript application/javascript application/json
</IfModule>
